name: 'GAE environment variable compiler with dynamic *.yaml'
description: 'Compiles and swaps environment variables in *.yaml with the minimal effort'
branding:
  icon: 'cloud'
  color: 'blue'
runs:
  using: 'docker'
  image: 'Dockerfile'
